server {
    listen 80;
    server_name msroseden.sriddha.com;

    root /var/www/msroseden/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Optimization for static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|otf)$ {
        expires 30d;
        add_header Cache-Control "public, no-transform";
    }

    access_log /var/log/nginx/msroseden_access.log;
    error_log /var/log/nginx/msroseden_error.log;
}
